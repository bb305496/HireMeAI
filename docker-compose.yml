version: '3.8'
services:
  frontend:
    image: nginx:alpine
    ports:
      - "8081:80"
    volumes:
      - ./current:/usr/share/nginx/html
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    restart: always
    networks:
      - app_default
networks:
  app_default:
    external: true
